# render.yaml
services:
  - type: web
    name: gemini-owl-api
    env: python
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn run_gemini:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PORT
        value: 8000
